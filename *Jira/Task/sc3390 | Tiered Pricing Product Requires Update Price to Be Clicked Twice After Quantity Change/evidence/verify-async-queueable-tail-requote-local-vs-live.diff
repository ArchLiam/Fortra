19c19
<  * @version 1.0 - New prehook implementation for pricing procedure integration
---
>  * @version 1.1 - Renewal maintenance COLA from stamped Base/Partner/Software discount components
105c105,112
<             return isOrderTransaction(items);
---
>             if (items.isEmpty()) return false;
>             Map<String, Object> firstItem = (Map<String, Object>) items.get(0);
>             Object dpObj = firstItem.get('dataPath');
>             if (!(dpObj instanceof List<Object>)) return false;
>             List<Object> dp = (List<Object>) dpObj;
>             if (dp.isEmpty()) return false;
>             String rootId = String.valueOf(dp.get(0));
>             return rootId != null && rootId.startsWith('801');
112,132d118
<      * @description Parsed-payload seam for Order detection. Pure of any live
<      *              Context API call so it can be unit-tested with a hand-built
<      *              SalesTransactionItem list. Returns the same sentinel (false)
<      *              the inline code returned for empty / malformed payloads, and
<      *              true only when the first item's dataPath[0] has the Order
<      *              prefix (801).
<      * @param items Parsed SalesTransactionItem node list
<      * @return true if the root entity Id has the Order prefix (801)
<      */
<     @TestVisible
<     private Boolean isOrderTransaction(List<Object> items) {
<         if (items == null || items.isEmpty()) return false;
<         Map<String, Object> firstItem = (Map<String, Object>) items.get(0);
<         Object dpObj = firstItem.get('dataPath');
<         if (!(dpObj instanceof List<Object>)) return false;
<         List<Object> dp = (List<Object>) dpObj;
<         if (dp.isEmpty()) return false;
<         String rootId = String.valueOf(dp.get(0));
<         return rootId != null && rootId.startsWith('801');
<     }
<     /**
174,176c160,270
<             List<Map<String, Object>> itemUpdates =
<                 processLineItems(rawLineItems, colaRulesMap, mycapRule);
<             processedCount = itemUpdates.size();
---
>             System.debug(LoggingLevel.INFO, 'Found ' + rawLineItems.size() + ' line items');
>             Set<Id> qliIds = new Set<Id>();
>             Map<Id, Map<String, Object>> qliIdToWrapper = new Map<Id, Map<String, Object>>();
>             for (Object rawItem : rawLineItems) {
>                 Map<String, Object> itemWrapper = (Map<String, Object>) rawItem;
>                 Object dpObj = itemWrapper.get('dataPath');
>                 if (dpObj instanceof List<Object>) {
>                     List<Object> dp = (List<Object>) dpObj;
>                     if (!dp.isEmpty()) {
>                         try {
>                             Id qliId = Id.valueOf(String.valueOf(dp.get(dp.size() - 1)));
>                             qliIds.add(qliId);
>                             qliIdToWrapper.put(qliId, itemWrapper);
>                         } catch (Exception e) {
>                         }
>                     }
>                 }
>             }
>             Map<Id, QuoteLineItem> renewalQlis = new Map<Id, QuoteLineItem>();
>             if (!qliIds.isEmpty()) {
>                 for (QuoteLineItem qli : [
>                     SELECT Id, QuoteActionId, QuoteAction.Type, QuoteAction.SourceAssetId,
>                            QuoteAction.SourceAsset.Price, QuoteAction.SourceAsset.Product2Id,
>                            QuoteAction.SourceAsset.Product2.Solution_Category__c,
>                            Default_COLA_Uplift_Percent__c
>                     FROM QuoteLineItem
>                     WHERE Id IN :qliIds
>                     AND QuoteAction.Type = 'Renew'
>                 ]) {
>                     renewalQlis.put(qli.Id, qli);
>                 }
>             }
>             System.debug(LoggingLevel.INFO, 'Found ' + renewalQlis.size() + ' renewal QLIs from ' + qliIds.size() + ' total');
>             Set<Id> assetIds = new Set<Id>();
>             for (QuoteLineItem rqli : renewalQlis.values()) {
>                 if (rqli.QuoteAction?.SourceAssetId != null) {
>                     assetIds.add(rqli.QuoteAction.SourceAssetId);
>                 }
>             }
>             Map<Id, ContractOverride> contractOverrides = getContractOverrides(assetIds);
>             List<Map<String, Object>> itemUpdates = new List<Map<String, Object>>();
>             for (Id qliId : renewalQlis.keySet()) {
>                 QuoteLineItem renewalQli = renewalQlis.get(qliId);
>                 Map<String, Object> itemWrapper = qliIdToWrapper.get(qliId);
>                 if (itemWrapper == null) continue;
>                 Map<String, Object> lineItem = extractLineItemData(itemWrapper);
>                 if (lineItem == null) continue;
>                 QuoteAction qa = renewalQli.QuoteAction;
>                 if (qa == null) continue;
>                 Decimal assetPrice = qa.SourceAsset?.Price;
>                 if (assetPrice == null || assetPrice <= 0) {
>                     System.debug(LoggingLevel.INFO, 'Skipping QLI ' + qliId + ' - no valid asset price');
>                     continue;
>                 }
>                 String solutionCategory = null;
>                 if (qa.SourceAsset?.Product2 != null) {
>                     solutionCategory = qa.SourceAsset.Product2.Solution_Category__c;
>                 }
>                 Decimal colaPercent = 0;
>                 String colaSource = 'CMDT Lookup';
>                 Decimal cmdtDefault = 0;
>                 if (String.isNotBlank(solutionCategory) && colaRulesMap.containsKey(solutionCategory)) {
>                     COLA_Uplift_Rules__mdt rule = colaRulesMap.get(solutionCategory);
>                     if (rule.Is_Active__c) {
>                         cmdtDefault = rule.Default_Uplift_Percent__c != null ? rule.Default_Uplift_Percent__c : 0;
>                     }
>                 }
>                 Decimal contractOverridePct = null;
>                 if (qa.SourceAssetId != null && contractOverrides.containsKey(qa.SourceAssetId)) {
>                     ContractOverride colaOverride = contractOverrides.get(qa.SourceAssetId);
>                     if (colaOverride.isValid) {
>                         contractOverridePct = colaOverride.overridePercent;
>                     }
>                 }
>                 Decimal lineOverridePercent = getDecimalFromTag(lineItem, 'COLA_Uplift_Percent__c');
>                 Boolean isEffectivelyOverridden = COLAUpliftHandler.isManualLineOverride(
>                     lineOverridePercent, cmdtDefault, null, contractOverridePct);
>                 if (isEffectivelyOverridden) {
>                     colaPercent = lineOverridePercent;
>                     colaSource = 'Line Override';
>                 } else if (contractOverridePct != null) {
>                     colaPercent = contractOverridePct;
>                     colaSource = 'Contract Override';
>                 }
>                 if (colaSource == 'CMDT Lookup') {
>                     colaPercent = cmdtDefault;
>                 }
>                 Decimal pricingTermCount = getDecimalFromTag(lineItem, 'PricingTermCount');
>                 if (mycapRule != null && mycapRule.Is_Active__c == true && colaSource == 'CMDT Lookup') {
>                     String pricingTermUnit = getStringFromTag(lineItem, 'PricingTermUnit');
>                     Decimal itemSubTerm = getDecimalFromTag(lineItem, 'ItemSubscriptionTerm');
>                     Boolean isAnnual = pricingTermUnit == 'Annual';
>                     Boolean isMultiYear = isAnnual
>                         && ((pricingTermCount != null && pricingTermCount > 1)
>                             || (itemSubTerm != null && itemSubTerm > 1));
>                     if (isMultiYear) {
>                         colaSource = 'MyCAP Default';
>                     }
>                 }
>                 Decimal colaAdjustedPrice = assetPrice * (1 + (colaPercent / 100));
>                 colaAdjustedPrice = colaAdjustedPrice.setScale(2, System.RoundingMode.HALF_UP);
>                 System.debug(LoggingLevel.INFO, 'Line item: AssetPrice=' + assetPrice +
>                     ', COLA%=' + colaPercent + ', Source=' + colaSource +
>                     ', AdjustedPrice=' + colaAdjustedPrice);
>                 String explainerText = buildExplainerText(colaPercent, solutionCategory, colaSource);
>                 Map<String, Object> itemUpdate = buildItemUpdate(itemWrapper, colaAdjustedPrice, colaPercent, explainerText, colaSource, assetPrice, solutionCategory);
>                 if (itemUpdate != null) {
>                     itemUpdates.add(itemUpdate);
>                     processedCount++;
>                 }
>             }
180,184c274,280
<             System.debug(LoggingLevel.INFO, 'processLineItems submitted ' + itemUpdates.size() + ' context updates');
<             if (this.pendingNetUnitPriceUpdates != null && !this.pendingNetUnitPriceUpdates.isEmpty()) {
<                 System.debug(LoggingLevel.INFO, 'SC-3350 submitting ' + this.pendingNetUnitPriceUpdates.size() + ' NetUnitPrice seed updates (isolated batch)');
<                 submitContextUpdates(ctx, ctxInstanceId, this.pendingNetUnitPriceUpdates);
<             }
---
>             processedCount += appendStampedRenewalMaintenanceUpdates(
>                 ctx,
>                 ctxInstanceId,
>                 qliIds,
>                 qliIdToWrapper
>             );
>             System.debug(LoggingLevel.INFO, 'processLineItems submitted ' + itemUpdates.size() + ' asset-renewal context updates');
191,203c287,288
<      * @description Parsed-payload seam for processLineItems. Given the already-
<      *              unwrapped SalesTransactionItem list, extracts QLI Ids from each
<      *              dataPath, queries the renewal QLI->QuoteAction->Asset graph,
<      *              resolves Contract overrides, and builds the list of context
<      *              node updates via the pure buildLineItemUpdates(...) loop.
<      *              Contains ZERO live Context API calls (queryTags /
<      *              updateContextAttributes) -- only QLI SOQL and the ACR SOQL
<      *              inside getContractOverrides, which are test-safe -- so the parse
<      *              + SOQL + loop section can be exercised with a hand-built item
<      *              list. Behavior is byte-for-byte equivalent to the original
<      *              inline section between the response unwrap and the submit call.
<      * @param rawLineItems Parsed SalesTransactionItem node list
<      * @return List of node-update maps (one per priced renewal line)
---
>      * @description Renewal maintenance lines without a renew asset use stamped prior-year
>      *              components. Writes COLACalculatedPrice__c for the DPP COLA formula.
206,209c291,295
<     private List<Map<String, Object>> processLineItems(
<         List<Object> rawLineItems,
<         Map<String, COLA_Uplift_Rules__mdt> colaRulesMap,
<         MyCAP_Rules__mdt mycapRule
---
>     private Integer appendStampedRenewalMaintenanceUpdates(
>         Context.IndustriesContext ctx,
>         String ctxInstanceId,
>         Set<Id> qliIds,
>         Map<Id, Map<String, Object>> qliIdToWrapper
211,227c297,298
<         System.debug(LoggingLevel.INFO, 'Found ' + rawLineItems.size() + ' line items');
<         Set<Id> qliIds = new Set<Id>();
<         Map<Id, Map<String, Object>> qliIdToWrapper = new Map<Id, Map<String, Object>>();
<         for (Object rawItem : rawLineItems) {
<             Map<String, Object> itemWrapper = (Map<String, Object>) rawItem;
<             Object dpObj = itemWrapper.get('dataPath');
<             if (dpObj instanceof List<Object>) {
<                 List<Object> dp = (List<Object>) dpObj;
<                 if (!dp.isEmpty()) {
<                     try {
<                         Id qliId = Id.valueOf(String.valueOf(dp.get(dp.size() - 1)));
<                         qliIds.add(qliId);
<                         qliIdToWrapper.put(qliId, itemWrapper);
<                     } catch (Exception e) {
<                     }
<                 }
<             }
---
>         if (qliIds == null || qliIds.isEmpty()) {
>             return 0;
229,273d299
<         Map<Id, QuoteLineItem> renewalQlis = new Map<Id, QuoteLineItem>();
<         if (!qliIds.isEmpty()) {
<             for (QuoteLineItem qli : [
<                 SELECT Id, QuoteActionId, QuoteAction.Type, QuoteAction.SourceAssetId,
<                        QuoteAction.SourceAsset.Price, QuoteAction.SourceAsset.Product2Id,
<                        QuoteAction.SourceAsset.Product2.Solution_Category__c,
<                        Default_COLA_Uplift_Percent__c, NetUnitPrice, ListPrice
<                 FROM QuoteLineItem
<                 WHERE Id IN :qliIds
<                 AND QuoteAction.Type = 'Renew'
<             ]) {
<                 renewalQlis.put(qli.Id, qli);
<             }
<         }
<         System.debug(LoggingLevel.INFO, 'Found ' + renewalQlis.size() + ' renewal QLIs from ' + qliIds.size() + ' total');
<         Set<Id> assetIds = new Set<Id>();
<         for (QuoteLineItem rqli : renewalQlis.values()) {
<             if (rqli.QuoteAction?.SourceAssetId != null) {
<                 assetIds.add(rqli.QuoteAction.SourceAssetId);
<             }
<         }
<         Map<Id, ContractOverride> contractOverrides = getContractOverrides(assetIds);
<         return buildLineItemUpdates(renewalQlis, qliIdToWrapper, colaRulesMap, contractOverrides, mycapRule);
<     }
<     /**
<      * @description Pure core-loop seam for processLineItems. Given the resolved
<      *              renewal QLI map, the QLI-Id->context-wrapper map, the CMDT rules,
<      *              the Contract overrides, and the MyCAP rule, builds the list of
<      *              context node updates (one per priced renewal line). Contains
<      *              ZERO live Context API / SOQL calls, so it is unit-testable with
<      *              hand-built inputs. Behavior is byte-for-byte equivalent to the
<      *              original inline loop: same skip conditions (missing wrapper,
<      *              null lineItem, null QuoteAction, no valid asset price), same
<      *              tier hierarchy, same MyCAP flagging, same price math, same
<      *              buildItemUpdate null-skip.
<      * @return List of node-update maps (one per line item that produced an update)
<      */
<     @TestVisible
<     private List<Map<String, Object>> buildLineItemUpdates(
<         Map<Id, QuoteLineItem> renewalQlis,
<         Map<Id, Map<String, Object>> qliIdToWrapper,
<         Map<String, COLA_Uplift_Rules__mdt> colaRulesMap,
<         Map<Id, ContractOverride> contractOverrides,
<         MyCAP_Rules__mdt mycapRule
<     ) {
275,295c301,313
<         this.pendingNetUnitPriceUpdates = new List<Map<String, Object>>();
<         for (Id qliId : renewalQlis.keySet()) {
<             QuoteLineItem renewalQli = renewalQlis.get(qliId);
<             Map<String, Object> itemWrapper = qliIdToWrapper.get(qliId);
<             if (itemWrapper == null) continue;
<             Map<String, Object> lineItem = extractLineItemData(itemWrapper);
<             if (lineItem == null) continue;
<             QuoteAction qa = renewalQli.QuoteAction;
<             if (qa == null) continue;
<             Decimal assetPrice = qa.SourceAsset?.Price;
<             if (assetPrice == null || assetPrice <= 0) {
<                 Decimal listFallback = renewalQli.getPopulatedFieldsAsMap().containsKey('ListPrice')
<                     ? (Decimal) renewalQli.get('ListPrice')
<                     : null;
<                 if (listFallback != null && listFallback > 0) {
<                     System.debug(LoggingLevel.INFO, 'QLI ' + qliId + ' - no asset price; falling back to ListPrice ' + listFallback);
<                     assetPrice = listFallback;
<                 } else {
<                     System.debug(LoggingLevel.INFO, 'Skipping QLI ' + qliId + ' - no valid asset price and no ListPrice fallback');
<                     continue;
<                 }
---
>         for (QuoteLineItem qli : [
>             SELECT Id, Base_Price__c, Prior_Partner_Discount__c, Prior_Discretionary_Discount__c,
>                    PartnerDiscountPercent, Discount, COLA_Uplift_Percent__c, Fortra_Product_Type__c,
>                    Quote.QuoteTypeText__c
>             FROM QuoteLineItem
>             WHERE Id IN :qliIds
>             AND Fortra_Product_Type__c = 'Renewal Maintenance'
>             AND Quote.QuoteTypeText__c = 'Renewal'
>             AND Base_Price__c > 0
>         ]) {
>             Map<String, Object> itemWrapper = qliIdToWrapper.get(qli.Id);
>             if (itemWrapper == null) {
>                 continue;
297,299c315,317
<             String solutionCategory = null;
<             if (qa.SourceAsset?.Product2 != null) {
<                 solutionCategory = qa.SourceAsset.Product2.Solution_Category__c;
---
>             Decimal colaNet = computeStampedMaintenanceColaNet(qli);
>             if (colaNet == null || colaNet <= 0) {
>                 continue;
301,348c319
<             Decimal colaPercent = 0;
<             String colaSource = 'CMDT Lookup';
<             Decimal cmdtDefault = 0;
<             if (String.isNotBlank(solutionCategory) && colaRulesMap.containsKey(solutionCategory)) {
<                 COLA_Uplift_Rules__mdt rule = colaRulesMap.get(solutionCategory);
<                 if (rule.Is_Active__c) {
<                     cmdtDefault = rule.Default_Uplift_Percent__c != null ? rule.Default_Uplift_Percent__c : 0;
<                 }
<             }
<             Decimal contractOverridePct = null;
<             if (qa.SourceAssetId != null && contractOverrides.containsKey(qa.SourceAssetId)) {
<                 ContractOverride colaOverride = contractOverrides.get(qa.SourceAssetId);
<                 if (colaOverride.isValid) {
<                     contractOverridePct = colaOverride.overridePercent;
<                 }
<             }
<             Decimal lineOverridePercent = getDecimalFromTag(lineItem, 'COLA_Uplift_Percent__c');
<             Boolean isEffectivelyOverridden = COLAUpliftHandler.isManualLineOverride(
<                 lineOverridePercent, cmdtDefault, null, contractOverridePct);
<             if (isEffectivelyOverridden) {
<                 colaPercent = lineOverridePercent;
<                 colaSource = 'Line Override';
<             } else if (contractOverridePct != null) {
<                 colaPercent = contractOverridePct;
<                 colaSource = 'Contract Override';
<             }
<             if (colaSource == 'CMDT Lookup') {
<                 colaPercent = cmdtDefault;
<             }
<             Decimal pricingTermCount = getDecimalFromTag(lineItem, 'PricingTermCount');
<             if (mycapRule != null && mycapRule.Is_Active__c == true && colaSource == 'CMDT Lookup') {
<                 String pricingTermUnit = getStringFromTag(lineItem, 'PricingTermUnit');
<                 Decimal itemSubTerm = getDecimalFromTag(lineItem, 'ItemSubscriptionTerm');
<                 Boolean isAnnual = pricingTermUnit == 'Annual';
<                 Boolean isMultiYear = isAnnual
<                     && ((pricingTermCount != null && pricingTermCount > 1)
<                         || (itemSubTerm != null && itemSubTerm > 1));
<                 if (isMultiYear) {
<                     colaSource = 'MyCAP Default';
<                 }
<             }
<             Decimal colaAdjustedPrice = assetPrice * (1 + (colaPercent / 100));
<             colaAdjustedPrice = colaAdjustedPrice.setScale(2, System.RoundingMode.HALF_UP);
<             System.debug(LoggingLevel.INFO, 'Line item: AssetPrice=' + assetPrice +
<                 ', COLA%=' + colaPercent + ', Source=' + colaSource +
<                 ', AdjustedPrice=' + colaAdjustedPrice);
<             String explainerText = buildExplainerText(colaPercent, solutionCategory, colaSource);
<             Map<String, Object> itemUpdate = buildItemUpdate(itemWrapper, colaAdjustedPrice, colaPercent, explainerText, colaSource, assetPrice, solutionCategory);
---
>             Map<String, Object> itemUpdate = buildMaintenanceColaItemUpdate(itemWrapper, qli, colaNet);
352,360d322
<             Decimal storedNet = renewalQli.getPopulatedFieldsAsMap().containsKey('NetUnitPrice')
<                 ? (Decimal) renewalQli.get('NetUnitPrice')
<                 : null;
<             if (storedNet == null || storedNet <= 0) {
<                 Map<String, Object> netUpdate = buildNetUnitPriceUpdate(itemWrapper, colaAdjustedPrice);
<                 if (netUpdate != null) {
<                     this.pendingNetUnitPriceUpdates.add(netUpdate);
<                 }
<             }
362c324,327
<         return itemUpdates;
---
>         if (!itemUpdates.isEmpty()) {
>             submitContextUpdates(ctx, ctxInstanceId, itemUpdates);
>         }
>         return itemUpdates.size();
364,366d328
<     /** SC-3350 net-price probe: net-seed updates built during buildLineItemUpdates,
<      *  submitted as a separate updateContextAttributes batch so a poison cannot kill
<      *  the COLA field writes. */
368c330,388
<     private List<Map<String, Object>> pendingNetUnitPriceUpdates = new List<Map<String, Object>>();
---
>     private Decimal computeStampedMaintenanceColaNet(QuoteLineItem qli) {
>         Decimal priorPartner = qli.Prior_Partner_Discount__c != null ? qli.Prior_Partner_Discount__c : 0;
>         Decimal priorDisc = qli.Prior_Discretionary_Discount__c != null && qli.Prior_Discretionary_Discount__c > 0
>             ? qli.Prior_Discretionary_Discount__c
>             : (qli.Discount != null && qli.Discount > 0
>                 ? ((qli.Base_Price__c - priorPartner) * (qli.Discount / 100)).setScale(2, System.RoundingMode.HALF_UP)
>                 : 0);
>         Decimal colaPercent = qli.COLA_Uplift_Percent__c != null ? qli.COLA_Uplift_Percent__c : 0;
>         Decimal preColaNet = qli.Base_Price__c - priorPartner - priorDisc;
>         if (preColaNet <= 0) {
>             return null;
>         }
>         return (preColaNet * (1 + (colaPercent / 100))).setScale(2, System.RoundingMode.HALF_UP);
>     }
>     @TestVisible
>     private Map<String, Object> buildMaintenanceColaItemUpdate(
>         Map<String, Object> itemWrapper,
>         QuoteLineItem qli,
>         Decimal colaNet
>     ) {
>         List<Object> dataPath;
>         if (itemWrapper.containsKey('dataPath')) {
>             dataPath = (List<Object>) itemWrapper.get('dataPath');
>         } else {
>             return null;
>         }
>         if (dataPath == null || dataPath.isEmpty()) {
>             return null;
>         }
>         List<Object> updatePath = dataPath.clone();
>         if (!updatePath.isEmpty()) {
>             updatePath.remove(0);
>         }
>         List<Map<String, Object>> attributes = new List<Map<String, Object>>{
>             new Map<String, Object>{ 'attributeName' => 'COLACalculatedPrice__c', 'attributeValue' => colaNet }
>         };
>         if (qli.PartnerDiscountPercent != null && qli.PartnerDiscountPercent > 0) {
>             attributes.add(new Map<String, Object>{
>                 'attributeName' => 'PartnerDiscountPercent',
>                 'attributeValue' => qli.PartnerDiscountPercent
>             });
>         } else if (qli.Prior_Partner_Discount__c != null && qli.Base_Price__c > 0) {
>             attributes.add(new Map<String, Object>{
>                 'attributeName' => 'PartnerDiscountPercent',
>                 'attributeValue' => ((qli.Prior_Partner_Discount__c / qli.Base_Price__c) * 100).setScale(2, System.RoundingMode.HALF_UP)
>             });
>         }
>         if (qli.Discount != null && qli.Discount > 0) {
>             attributes.add(new Map<String, Object>{
>                 'attributeName' => 'Discount',
>                 'attributeValue' => qli.Discount
>             });
>         }
>         System.debug(LoggingLevel.INFO, 'RENEWAL MAINT COLA PREHOOK: QLI ' + qli.Id + ' -> ' + colaNet);
>         return new Map<String, Object>{
>             'nodePath' => new Map<String, Object>{ 'dataPath' => updatePath },
>             'attributes' => attributes
>         };
>     }
449,488d468
<      * @description SC-3350 net-price test: build an ISOLATED context update that seeds
<      *              NetUnitPrice = the COLA-adjusted price for a renewal line. Kept in its
<      *              own batch so, if NetUnitPrice lacks a hydration mapping and poisons the
<      *              call, the COLA field writes in buildItemUpdate are unaffected. Probe to
<      *              determine whether a prehook-level NetUnitPrice write commits to the
<      *              net waterfall (vs being overwritten by PricingSettings).
<      */
<     @TestVisible
<     private Map<String, Object> buildNetUnitPriceUpdate(
<         Map<String, Object> itemWrapper,
<         Decimal colaPrice
<     ) {
<         if (colaPrice == null || colaPrice <= 0) {
<             return null;
<         }
<         List<Object> dataPath;
<         if (itemWrapper.containsKey('dataPath')) {
<             dataPath = (List<Object>) itemWrapper.get('dataPath');
<         } else {
<             return null;
<         }
<         if (dataPath == null || dataPath.isEmpty()) {
<             return null;
<         }
<         List<Object> updatePath = dataPath.clone();
<         if (!updatePath.isEmpty()) {
<             updatePath.remove(0);
<         }
<         List<Map<String, Object>> attributes = new List<Map<String, Object>>{
<             new Map<String, Object>{
<                 'attributeName' => 'NetUnitPrice',
<                 'attributeValue' => colaPrice
<             }
<         };
<         Map<String, Object> result = new Map<String, Object>();
<         result.put('nodePath', new Map<String, Object>{ 'dataPath' => updatePath });
<         result.put('attributes', attributes);
<         return result;
<     }
<     /**
520d499
<     @TestVisible
588,609d566
<         return buildOverrideMap(relationships);
<     }
<     /**
<      * @description Pure validity-matrix seam for getContractOverrides. Given the
<      *              AssetContractRelationship rows (each carrying a Contract child
<      *              relationship), builds the Asset-Id -> ContractOverride map.
<      *              Contains ZERO live SOQL so it is unit-testable with in-memory
<      *              AssetContractRelationship rows + a real activated Contract.
<      *              Behavior is byte-for-byte equivalent to the original inline
<      *              loop: skips null/no-override-percent contracts, treats a null
<      *              persist-until as a valid one-time override, includes future
<      *              persist dates, excludes expired (past) persist dates, and
<      *              keeps the first relationship encountered per Asset.
<      * @param relationships AssetContractRelationship rows (queried or in-memory)
<      * @return Map of ContractOverride wrappers keyed by Asset Id
<      */
<     @TestVisible
<     private Map<Id, ContractOverride> buildOverrideMap(List<SObject> relationships) {
<         Map<Id, ContractOverride> overrideMap = new Map<Id, ContractOverride>();
<         if (relationships == null) {
<             return overrideMap;
<         }
637d593
<     @TestVisible
642d597
<     @TestVisible
657d611
<     @TestVisible
721,746d674
<         try {
<             Context.IndustriesContext ctx = new Context.IndustriesContext();
<             List<Object> rawLineItems = fetchSalesTransactionItems(ctx, ctxInstanceId);
<             return processMyCAPEligibility(rawLineItems, rule);
<         } catch (Exception e) {
<             System.debug(LoggingLevel.ERROR, 'MyCAP processing error: ' + e.getMessage());
<             System.debug(LoggingLevel.ERROR, 'Stack: ' + e.getStackTraceString());
<             return 0;
<         }
<     }
<     /**
<      * @description Parsed-payload seam for processMyCAPEligibility. Given the
<      *              already-fetched SalesTransactionItem list and the MyCAP rule,
<      *              runs the eligibility evaluation, flag collection, and the
<      *              MyCAPFlagApplier enqueue. Contains ZERO live Context API calls
<      *              (queryTags / updateContextAttributes) -- only QLI / attribute
<      *              SOQL and System.enqueueJob, which are test-safe -- so the real
<      *              MyCAP path can be exercised with a hand-built item list.
<      *              Behavior is preserved: same renewal gate, same qualifying
<      *              criteria, same flag-true/flag-false collection, same enqueue.
<      * @param rawLineItems Parsed SalesTransactionItem node list
<      * @param rule Global MyCAP rule CMDT record (already null/active checked)
<      * @return Number of distinct Quotes evaluated
<      */
<     @TestVisible
<     private Integer processMyCAPEligibility(List<Object> rawLineItems, MyCAP_Rules__mdt rule) {
751a680,681
>             Context.IndustriesContext ctx = new Context.IndustriesContext();
>             List<Object> rawLineItems = fetchSalesTransactionItems(ctx, ctxInstanceId);
956c886
<      *              engine write-back -- no QLI DML here to avoid repricing loops.
---
>      *              engine write-back — no QLI DML here to avoid repricing loops.
