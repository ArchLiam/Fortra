1276a1277,1279
>         // J-06: true when partnerPercent is the authoritative New-Maintenance band resolved from
>         // the billing partner's Discount model (not a carried license band or stamped value).
>         public Boolean partnerBandResolved = false;
1349a1353,1355
>                 // J-06: derived New-Maintenance partner discount must use the billing partner's
>                 // New-Maintenance band, not the license contributor's carried Software band.
>                 Map<Id, Decimal> nmBandByQuote = resolveNewMaintenancePartnerBandByQuote(qlis.values());
1372,1374c1378,1382
<                     Decimal partnerPercent = contributor != null && contributor.partnerPercent != null
<                         ? contributor.partnerPercent
<                         : qli.PartnerDiscountPercent;
---
>                     Decimal partnerPercent = nmBandByQuote.get(qli.QuoteId) != null
>                         ? nmBandByQuote.get(qli.QuoteId)
>                         : (contributor != null && contributor.partnerPercent != null
>                             ? contributor.partnerPercent
>                             : qli.PartnerDiscountPercent);
1381a1390
>                     byId.get(qli.Id).partnerBandResolved = nmBandByQuote.get(qli.QuoteId) != null;
1399a1409
>                 Map<Id, Decimal> nmBandByOrder = resolveNewMaintenancePartnerBandByOrder(ois.values());
1422,1423c1432,1435
<                         contributorNet, contributorNetFromPerpetual, oi.Source_List_Price__c, oi.PartnerDiscountPercent, null,
<                         oi.Quantity, rate, oi.OrderId));
---
>                         contributorNet, contributorNetFromPerpetual, oi.Source_List_Price__c,
>                         (nmBandByOrder.get(oi.OrderId) != null ? nmBandByOrder.get(oi.OrderId) : oi.PartnerDiscountPercent),
>                         null, oi.Quantity, rate, oi.OrderId));
>                     byId.get(oi.Id).partnerBandResolved = nmBandByOrder.get(oi.OrderId) != null;
1428a1441,1561
>     }
> 
>     /**
>      * @description J-06: New-Maintenance partner discount band per Quote, read directly from the
>      *              billing partner's active Discount pricing model (New_Maintenance_Percent__c)
>      *              rather than the license contributor's Software band. Resolved via SOQL on the
>      *              Quote so it is independent of pricing-context header tags. Quotes that do not
>      *              use Discount partner pricing are omitted; callers then keep existing behavior.
>      */
>     @TestVisible
>     private Map<Id, Decimal> resolveNewMaintenancePartnerBandByQuote(List<QuoteLineItem> maintLines) {
>         Map<Id, Decimal> bandByQuote = new Map<Id, Decimal>();
>         if (maintLines == null || maintLines.isEmpty()) {
>             return bandByQuote;
>         }
>         Set<Id> quoteIds = new Set<Id>();
>         for (QuoteLineItem qli : maintLines) {
>             if (qli.QuoteId != null) {
>                 quoteIds.add(qli.QuoteId);
>             }
>         }
>         if (quoteIds.isEmpty()) {
>             return bandByQuote;
>         }
>         Map<Id, Quote> quotes = new Map<Id, Quote>([
>             SELECT Id, Billing_Partner__c, Partner_Pricing_Model__c
>             FROM Quote
>             WHERE Id IN :quoteIds
>         ]);
>         Set<Id> billingPartnerIds = new Set<Id>();
>         for (Quote q : quotes.values()) {
>             if (q.Partner_Pricing_Model__c == 'Discount' && q.Billing_Partner__c != null) {
>                 billingPartnerIds.add(q.Billing_Partner__c);
>             }
>         }
>         if (billingPartnerIds.isEmpty()) {
>             return bandByQuote;
>         }
>         Map<Id, Partner_Pricing_Model__c> models =
>             PartnerPricingService.getPricingModels(billingPartnerIds, 'Discount');
>         Decimal band;
>         for (Quote q : quotes.values()) {
>             band = newMaintenanceBand(q.Partner_Pricing_Model__c, q.Billing_Partner__c, models);
>             if (band != null) {
>                 bandByQuote.put(q.Id, band);
>             }
>         }
>         return bandByQuote;
>     }
> 
>     /**
>      * @description J-06: New-Maintenance partner discount band per Order, resolved from the source
>      *              Quote linked on the Order (same Discount-model logic as the Quote path).
>      */
>     @TestVisible
>     private Map<Id, Decimal> resolveNewMaintenancePartnerBandByOrder(List<OrderItem> maintLines) {
>         Map<Id, Decimal> bandByOrder = new Map<Id, Decimal>();
>         if (maintLines == null || maintLines.isEmpty()) {
>             return bandByOrder;
>         }
>         Set<Id> orderIds = new Set<Id>();
>         for (OrderItem oi : maintLines) {
>             if (oi.OrderId != null) {
>                 orderIds.add(oi.OrderId);
>             }
>         }
>         if (orderIds.isEmpty()) {
>             return bandByOrder;
>         }
>         Map<Id, Order> orders = new Map<Id, Order>([
>             SELECT Id, Quote.Billing_Partner__c, Quote.Partner_Pricing_Model__c
>             FROM Order
>             WHERE Id IN :orderIds
>         ]);
>         Set<Id> billingPartnerIds = new Set<Id>();
>         for (Order ord : orders.values()) {
>             if (ord.Quote != null
>                 && ord.Quote.Partner_Pricing_Model__c == 'Discount'
>                 && ord.Quote.Billing_Partner__c != null) {
>                 billingPartnerIds.add(ord.Quote.Billing_Partner__c);
>             }
>         }
>         if (billingPartnerIds.isEmpty()) {
>             return bandByOrder;
>         }
>         Map<Id, Partner_Pricing_Model__c> models =
>             PartnerPricingService.getPricingModels(billingPartnerIds, 'Discount');
>         Decimal band;
>         for (Order ord : orders.values()) {
>             if (ord.Quote == null) {
>                 continue;
>             }
>             band = newMaintenanceBand(
>                 ord.Quote.Partner_Pricing_Model__c, ord.Quote.Billing_Partner__c, models);
>             if (band != null) {
>                 bandByOrder.put(ord.Id, band);
>             }
>         }
>         return bandByOrder;
>     }
> 
>     /**
>      * @description Resolve the New-Maintenance discount band from a billing partner's Discount
>      *              pricing model. Returns null unless the model type is Discount and a positive
>      *              New_Maintenance_Percent__c resolves, so callers fall back to existing behavior.
>      */
>     @TestVisible
>     private Decimal newMaintenanceBand(
>         String pricingModel,
>         Id billingPartnerId,
>         Map<Id, Partner_Pricing_Model__c> models
>     ) {
>         if (pricingModel != 'Discount' || billingPartnerId == null || models == null) {
>             return null;
>         }
>         Partner_Pricing_Model__c model = models.get(billingPartnerId);
>         if (model == null) {
>             return null;
>         }
>         Decimal band = PartnerPricingService.getMarginForProductType(model, 'New Maintenance');
>         return (band != null && band > 0) ? band : null;
1881,1882c2014,2017
<         nets.partnerPercent = resolveStampedPartnerPercent(lineItem);
<         if (nets.partnerPercent == null || nets.partnerPercent <= 0) {
---
>         if (maintInputs.partnerBandResolved
>             && maintInputs.partnerPercent != null && maintInputs.partnerPercent > 0) {
>             // J-06: the authoritative New-Maintenance band from the partner Discount model overrides
>             // the license band the prehook stamped onto this derived-maintenance line.
1883a2019,2023
>         } else {
>             nets.partnerPercent = resolveStampedPartnerPercent(lineItem);
>             if (nets.partnerPercent == null || nets.partnerPercent <= 0) {
>                 nets.partnerPercent = maintInputs.partnerPercent;
>             }
