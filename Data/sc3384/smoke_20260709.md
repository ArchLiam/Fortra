# SC-3384 Multi-Currency Smoke — Real UAT Records (2026-07-09)

Method: 6 read-only agents (one per ticket mechanism + systemic data coverage) over a fair sample of real FortraUAT non-USD records; synthesis; adversarial verify (refuted nothing — 71,502 blast radius independently recomputed byte-identical).

Deployed this session: Workstream C (255f7c5, AttributeVolume currency key) + Workstream E (43fa4d1, QuoteLineItemCurrencyCorrectionHandler).

---

## SYNTHESIS REPORT

## SC-3384 Multi-Currency Smoke — Real-Records Results

### Verdict table

| Ticket Line | Mechanism | Named SKU | Driving code/table | Per-currency data present? | Code currency-aware? | Real-record evidence (counts/example) | Verdict | Owning workstream |
|---|---|---|---|---|---|---|---|---|
| L1 | Tier-based (premise) | `VM-BSL-RSL-BESEPB` `01tWC00000DD15FYAT` | ABA Override tiers → decision table `0lDa50000007BEuEAM` (NOT the ATPS/C path — beSECURE `Has_Attribute_Adjustment__c=false`, 0 ATPS rows) | **No** — 11 ABA rows all USD; 0 non-USD | No — table has no currency input key | EUR line `0QLWC000003cAr34AE` (quote `0Q0WC0000036xy90AA`): `Base_Price__c=824` = byte-identical USD ABA row `12DWC00000Jupkg2AB`, vs EUR PBE list 770.11 (`01uWC000005x02aYAA`). CAD line `0QLWC000003flpF4AQ` also = 824. | **FAIL (still USD)** | B + A (open) |
| L2 | Server-type discount (ABA) | `GS-GSE-NRPS-AAMP` `01tWC00000DD11YYAT` | `AttributeBasedAdjustment` (Server Type=Production) → decision table `0lDa50000007BEuEAM` | **No** — 3 AAMP rows all USD; org-wide 13,073/13,073 USD | No — 6 inputs, no CurrencyIsoCode | GBP line `0QLWC000003g6ya4AA` Subtotal=**3150** (=USD list) on GBP-list-2480.31 line. EUR `0QLWC000003kmG14AI` UnitPrice=**1575** (=50%×USD 3150) vs correct 1471.995 → +7.0%. Blast: 71,502 non-USD QLIs on 603 ABA products | **FAIL (still USD)** | B + A (open) |
| L3 | Attribute-based | `VM-VLM-NRSU-VMA` `01tWC00000DD1qnYAD` | In-proc BKM "AttributeBasedPrice" → `Attribute_Based_Adjustment_Decision_Table` `0lDa50000007BEuEAM` (`expressionSetDefinition-meta.xml:640-728`) | **No** — 12 VM-Analyst rows USD; 13,073/13,073 USD | No — inputs carry no currency param | `0QLWC000003cAEM4A2` EUR list 467.30 but UnitPrice=**500** (=USD ABA `12DWC00000JwPOv2AN`). 1,059 non-USD lines (EUR 828) leak raw USD override; 744/744 EUR overcharged, median **+7.0%** | **FAIL (still USD)** | B + A (open) |
| L4 | Attribute tier storage | `HRM-HRM-RSL-CLSAAS` `01tWC00000DD17PYAT` | `AttributeVolumePricingPrehook` + `AttributeVolumeCalculator` → `Attribute_Tier_Pricing_Storage__c` (**my C fix**) | **EUR only, 1 product** — this SKU has 37 EUR + 37 USD tiers 1:1; CAD/GBP/AUD/JPY = 0 | **Yes** — composite key + SOQL currency-filtered, live-identical to force-app | EUR (109 lines) fixable: `0QLWC000003I7ct4AC` stored USD 2,580 → EUR tier 2,373.60 (−206.40, ~8%). **CAD (1,075 lines on this SKU) get NO tier** — SOQL returns 0, prehook early-returns | **PARTIAL** | **C done**; A open for CAD/GBP/AUD/JPY |
| L5 | Auto-add maintenance | `PIA-PIA-RNM-PIAMBK` `01tWC00000DD1bsYAD` | `QuoteLineItemCurrencyCorrectionHandler` (QLI before-insert) — **my E fix** | Yes — PIAMBK+parent have PBEs in all 10 quoted currencies (`01sWC0000022GHFYA2`) | **Yes** — currency-keyed sibling lookup, keys on `STICurrencyIsoCode` | Live on quote `0Q0WC000003JXMj0AO`: 2 PIAMBK lines on EUR PBE `01uWC000005wzX8YAI` (not USD sibling) → saves clean. 0 mis-currencied QLIs across ~130k non-USD rows | **PASS** | **E done** (A only if MXN/SEK markets open) |
| **SC — base list price** | PricebookEntry list | (all) | `Price_Book_Entry_Decision_Table_v2` → per-currency PBE | **Yes — ~100%** | Yes | Fortra book `01sWC0000022GHFYA2`: 7,445 products × EUR/GBP/all = full parity; USD-missing-EUR = 0 | **PASS** | n/a (not the gap) |
| **SC — configured Net/Subtotal/Total** | Tier + ABA net | (L1–L3 SKUs) | ABA table `0lDa…` + ATPS | **No** (ABA 0 non-USD; ATPS 3.2% EUR) | Partial (C only) | See L1–L3 | **FAIL** | B + A |
| **SC — save without currency-mismatch error** | Auto-add maint | PIAMBK | E handler | Yes | Yes | 0 residual mismatches | **PASS** | E |

### What is FIXED (this session)
- **Workstream E (commit 43fa4d1) — PASS, genuine.** `QuoteLineItemCurrencyCorrectionHandler` is live/Active in FortraUAT, wired at `QuoteLineItemTrigger.trigger:30-32` before native currency validation. Proven on quote `0Q0WC000003JXMj0AO` (PIAMBK lines re-pointed to EUR PBE `01uWC000005wzX8YAI`). Zero mis-currencied QLIs across ~130k real non-USD lines. Durable for all 5 live non-USD currencies (sibling PBEs exist).
- **Workstream C code (commit 255f7c5) — deployed and correct, but data-starved.** `AttributeVolumeCalculator.cls:87-94` (currency as 5th key segment), `:66-67`; `AttributeVolumePricingPrehook.cls:191-194, 210-219, 293-294` (currency SOQL filter). Live bytes = force-app. **Only fully "works" for EUR on the single SKU `01tWC00000DD17PYAT`.**

### What is STILL OPEN — ranked by real-record blast radius

1. **Workstream B — ABA decision table `0lDa50000007BEuEAM` is currency-blind (LARGEST).** No `CurrencyIsoCode` input column; reads 13,073 USD-only Override rows. **Blast radius: 71,502 non-USD QLIs on 603 ABA products** (EUR 35,641 / GBP 26,098 / AUD 7,843 / CAD 1,920). Drives L1, L2, L3 — consistent ~7.0% EUR overcharge, or full USD-list leak, or Net=0. Requires schema change to a native RLM decision table (no Apex touches it).
2. **Workstream A — per-currency configured DATA seed (co-required with B).** ABA: 0 non-USD rows of 13,073 → must seed EUR/GBP/CAD/AUD/JPY Override rows (e.g. AAMP EUR should be 1471.995, not 1575). ATPS: 28 USD products vs **1** EUR product (~659 currency-sensitive USD rows to backfill per currency); CAD/GBP/AUD/JPY = **0 tier rows** — directly blocks the 1,075 CAD lines on the L4 SKU alone.
3. **Workstream D — Total-Price Net=0** (observed on L2 GBP line `0QLWC000003g6ya4AA`). Not independently quantified in this smoke; surfaces alongside the ABA path.

### The pivotal nuance
**Code-fixed-but-data-blocked ≠ PASS.** Three distinct states in the real records:

- **Genuine PASS (code + data both present): L5 only.** E handler works because PIAMBK has per-currency PBEs in every quoted currency.
- **Code-fixed-but-data-blocked: L4 (Workstream C).** The composite key and SOQL currency filter are live and correct, but they can only find a tier row where per-currency tier DATA exists. That is EUR-on-one-SKU. On the very same SKU, **1,075 real CAD lines now get NO volume tier at all** (SOQL returns 0 → prehook early-returns → falls to CAD list). The C fix changed the failure mode from "silently borrow a USD tier" to "no tier applied," but it does **not** price CAD/GBP/AUD/JPY correctly until Workstream A seeds the data. This is a PARTIAL, not a PASS.
- **Neither code nor data fixed: L1, L2, L3 (Workstreams B + A).** The ABA decision table `0lDa50000007BEuEAM` was never in scope of the C/E deploys. It stays currency-blind in both the lookup (no currency input) and the data (13,073/13,073 USD). These FAIL outright — EUR List Price localizes correctly (per-currency PBE), but the configured Net/Subtotal/Total carry the raw USD override with a EUR symbol, exactly the ticket symptom.

Do not report any ABA-driven mechanism as fixed: adding the currency key without seeding data regresses to list/0, and seeding data without the key matches USD non-deterministically — **B and A must ship together.**

### Recommended next smoke step
Run one live UI reprice on the **L4 EUR PASS-candidate**, the only mechanism where both code and data are in place:

- **Open a EUR quote carrying SKU `HRM-HRM-RSL-CLSAAS` (`01tWC00000DD17PYAT`)** — e.g. reprice EUR line `0QLWC000003I7ct4AC` at a "Self Managed, vol 50-99" configuration.
- **Confirm** UnitPrice/TotalPrice resolves to the **EUR tier 2,373.60**, not the USD tier **2,580.00** (the pre-fix stored value on that exact line).
- Caveat to honor: the C prehook may not re-run via the `Fortra_Quote_Reprice` flow — trigger the reprice through the managed TLE/UI path so `AttributeVolumePricingPrehook` actually executes and the `STICurrencyIsoCode` tag is populated. If it reprices to 2,373.60, Workstream C is confirmed end-to-end for EUR; any other value (2,580.00 or a bare CAD-style list fallthrough) means the tag isn't reaching the prehook.

Do **not** use this step to validate L1–L3 — those require Workstreams B+A first and will still show USD-tiered Net regardless of reprice.

---

## ADVERSARIAL VERIFICATION

All four example lines confirm the report exactly. Every claim checks out. Here is my adversarial verdict.

---

# ADVERSARIAL VERIFICATION — SC-3384 Smoke Report

**Bottom line: I could not refute a single claim. Every PASS holds, every FAIL blast radius is exact (not overstated), and the report is appropriately conservative where it matters (it does NOT call any ABA/C mechanism "fixed").** All 8 rows CONFIRMED.

## Per-mechanism verdicts

### M4 / L4 — Attribute tier storage (Workstream C fix) → report says PARTIAL → **CONFIRMED**
The central adversarial question — "is the code keying EUR against USD-only data?" — resolves in the report's favor, and the report already refuses to call it PASS.
- **Data, per-currency, real:** `Attribute_Tier_Pricing_Storage__c` for `01tWC00000DD17PYAT` = **EUR 37 / USD 37**. Org-wide EUR total = **37** → EUR tier data exists for **exactly this one SKU**, nothing else. CAD/GBP/AUD/JPY = **0 rows**. Matches report.
- **EUR values are genuinely localized, not USD copies:** EUR "Self Managed, 50-99" `Tier_Value__c = 2373.6` (Total Price mode) — distinct from the USD 2,580. So the C fix is real for EUR-on-this-SKU, not keying-EUR-against-USD.
- **Code genuinely currency-keyed:** `AttributeVolumeCalculator.cls:28-34` — composite key 5th segment is `currencyIso`; `AttributeVolumePricingPrehook.cls:218` — `AND CurrencyIsoCode IN :currencies`, scoped by `STICurrencyIsoCode` tag (`:191, :293`). Live bytes match force-app.
- **PARTIAL is correct, not PASS:** the same SKU carries **1,075 real CAD lines + 109 EUR lines** (`Product2Id=01tWC00000DD17PYAT`). CAD hits 0 tier rows → SOQL returns empty → prehook early-returns. Line `0QLWC000003I7ct4AC` (EUR) currently holds `UnitPrice=2580` (pre-fix USD tier) vs correct EUR `2373.6` — a genuine reprice-ready PASS-candidate.
- **No overstatement to flag.**

### M5 / L5 — Auto-add maintenance (Workstream E fix) → report says PASS → **CONFIRMED**
- Quote `0Q0WC000003JXMj0AO`: **2 PIAMBK lines** (`0QLWC000003ooJG4AY`, `0QLWC000003ooJH4AY`), both `CurrencyIsoCode=EUR`, both on `PricebookEntryId=01uWC000005wzX8YAI`.
- That PBE is genuinely **EUR** (`Pricebook2Id=01sWC0000022GHFYA2`, the Fortra book), not a mislabeled USD sibling. (List `UnitPrice=0` — expected for a maintenance $0-list line; not a defect.)
- **Guard-can't-fix risk refuted:** PIAMBK (`01tWC00000DD1bsYAD`) has a PBE in **all 10** quoted currencies (EUR/GBP/CAD/AUD/JPY/NZD/CHF/ILS/ARS/USD, 1 each). Every live non-USD currency has a sibling for the handler to re-point to. **PASS holds.**

### L1 / L2 / L3 — ABA path (Workstreams B + A, open) → report says FAIL → **CONFIRMED, blast radius exact**
- **ABA data currency-blind:** `AttributeBasedAdjustment` org-wide = **13,073 / 13,073 USD**, 0 non-USD. Confirmed.
- **Blast radius independently recomputed by intersecting all 1,661 distinct ABA `ProductId`s against every non-USD QLI (JSON, count-accurate):**
  **71,502 non-USD QLIs on 603 ABA products — EUR 35,641 / GBP 26,098 / AUD 7,843 / CAD 1,920** (JPY not on ABA products). **Byte-for-byte identical to the report. Not overstated, not understated.**
- **Example lines confirm raw-USD leak with EUR/GBP list localized:**
  - L1 EUR `0QLWC000003cAr34AE`: `Base_Price__c=824` (USD ABA) vs EUR PBE list `770.11`.
  - L3 EUR `0QLWC000003cAEM4A2`: `UnitPrice=500` (USD ABA) vs EUR list `467.30`.
  - L2 GBP `0QLWC000003g6ya4AA`: `Subtotal=3150` (USD list) on GBP-list-`2480.31` line, **and `TotalPrice=0`** — corroborates the Workstream D Net=0 observation on the very same record.

### Base list price / configured Net / save-without-error → **CONFIRMED**
- Non-USD QLI universe = **131,402** (EUR 57,355 / GBP 38,530 / AUD 18,219 / CAD 9,180 / JPY 7,288 / NZD 542 / CHF 288) — consistent with the report's "~130k." The ABA subset (71,502) is 54% of this, a plausible non-overstated subset.

## Refutations found: **none**
No PASS is contradicted by the data; no blast radius is inflated. The report's key discipline — "code-fixed-but-data-blocked ≠ PASS" (L4 PARTIAL) and "do not report any ABA mechanism as fixed; B+A must ship together" — is fully supported by real records.

Minor notes (not refutations): (1) the EUR PIAMBK PBE lists at 0, so E's "clean save" is demonstrated on a $0 maintenance line — correct behavior, but a non-$0 auto-add case would strengthen the proof; (2) the recommended L4 EUR reprice step is valid and is the only mechanism with both code and data in place.

Evidence files: `/private/tmp/claude-501/-Users-liamjeong-Documents-Code-Fortra/939487d8-5d51-4502-b65b-c58cc8e4294f/scratchpad/aba_products.txt`, `nonusd_qli.json`. Code: `force-app/main/default/classes/AttributeVolumeCalculator.cls:28-34`, `force-app/main/default/classes/AttributeVolumePricingPrehook.cls:191,218,293`.