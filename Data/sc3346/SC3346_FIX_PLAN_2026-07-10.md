# SC-3346 Staged Fix Plan (Path B) — Owner Decision Package
*Generated 2026-07-10 via a grounded orientation workflow (RCA + refuted-attempts + in-flight code + change surface), adversarially verified as SOUND (does not reintroduce any of the 19 refuted approaches).*

> This is the authoritative execution plan. It supersedes ad-hoc attempts. Every stage has entry/exit gates, reversibility, and the specific owner decision it needs. The "Do NOT" list is load-bearing — each item was empirically refuted.

<!-- Full plan body pasted from the workflow synthesis; see the sections below. -->

## Key state reframe (why this is not "from zero")
- **Path B is already ~70% built and deployed to UAT at near-full scale**: 1,681 TermDefined New-Maint PSMOs + 33,620 PBEs provisioned (`RenewalMaintenanceCatalogProvisionBatch` + `RenewalMaintenanceProvisionService`) — but **untracked in git** and **unproven at scale**.
- **The false-green trap**: 31,016 / 33,620 TermDefined mirror PBEs are `UnitPrice=0` (batch copied $0 from the OneTime source). Only **one** line (BoKS `76.57`) is end-to-end proven. A renewal-count dropping to 0 would *look* fixed while silently re-creating the defect under the new selling model.
- **Live impact**: 132,823 / 486,584 renewal-maint lines at $0 (27% all-time; 13/18 = 72% last 30d). Durable live-actionable backlog ≈ 7,203 unpriced lines / 687 accts (the ~120K RRM $0 rows are 98.9% legacy noise).

## Stages (each gated; see full text for entry/exit/reversibility/owner-decision)
- **Stage 0 — Interim LOUD GUARD (ship now):** flip silent $0 → visible flag + hard Workday-submission block. No engine write. UAT-only. Reversible.
  - **CORRECTION 2026-07-10 (owner caught):** `OrderSubmissionValidator` is NOT the gate — it only runs on the manual "Order Complete" quick action (`Fortra_Order_Submission_Check` screen flow: validate + toast) and is bypassed by any headless/record-triggered submission. **The real Workday choke point is record-triggered on Order Update:** `Order_After_Update_Platform_Events` (publishes `Order_Completed_WD__e` when `Workday_Sync_Status__c`→Pending) + `Order_Submission_to_Revenue_Orchestrator` (`Status='Activated'`).
  - **Corrected hard block:** gate the publish of `Order_Completed_WD__e` — if any OrderItem is renewal-maintenance (`Fortra_Product_Type__c LIKE '%Maintenance%'`, `Qty>0`, `UnitPrice<=0`) → HOLD the push (`Workday_Sync_Status__c='Held'` + named-SKU notification) [policy A], or block activation [policy B]. Touches a load-bearing submission flow → test carefully.
  - **Soft flag (keep):** `QuoteLineItem.Maintenance_Pricing_Failed__c` stamped after `Fortra_Quote_Reprice` — earliest signal, fires on the renewal path. Manual `OrderSubmissionValidator` extension = rep-facing early warning only, not the gate.
- **Stage 1 — Freeze & attribute in-flight Path B:** snapshot org + commit the untracked/modified work as-is; a **blocking** canary proves whether the one proven commit (BoKS 76.57) came from native `LastTransaction` or from the still-live custom COLA stack masking it.
- **Stage 2 — Prove committed-NET at scale:** validate on committed NET (never `UnitPrice`, never `PBE.UnitPrice=$0`) across SKU × currency (10×2) × derived-sibling. Decide Gap B (derived-sibling PBEs), platinum fallback, 37-flip disposition. **+ (verifier gap 1) add an axis confirming target assets carry the `AssetStatePeriod`/`AssetActionSource` the native Renew join needs.**
- **Stage 3 — Wire first-renewal coverage + close double-path:** today only 2nd+ renewals get a TermDefined line (752 pure-first-renewal accts unrescued). Add coverage (DML-swap + catalog-commit paths have ZERO coverage; prod deploy coverage-blocked). Deactivate-first, never wholesale-delete `RenewalMaintenanceAutoAddHandler` / `PartnerNetPricePosthook`.
- **Stage 4 — Order-side / Workday parity:** prove born-net carries Quote→Order→Workday non-$0 end to end.
- **Stage 5 — Native-uplift build in SANDBOX (deletes COLA LAST):** native plumbing is **100% empty** (0 PriceRevisionPolicy rows, Asset.PricingSource null on all 430,717 assets). Parity-prove in sandbox with custom stack still live; deactivate-first, delete-last.
- **Stage 6 — Backlog drain by RE-PROVISIONING (never mass-reprice):** Draft/no-Order only; pilot ONE line; if Apex-insert-on-persisted-quote commits $0 → PIVOT to `initiateRenewal`. Never touch 982 Approved / 30 Won / 208 Ordered.
- **Stage 7 — PROD from-zero cutover (separately authorized):** the entire SC-3346 layer is absent in PROD; minimally-scoped manifest; Stage 0 guard ships to PROD first as backstop.

## Do NOT (empirically refuted — reason)
- Write `NetUnitPrice`/`NetTotalPrice`/`TotalPrice` via posthook/`updateContextAttributes`/DML/prehook/canvas — engine-owned (createable=false); write returns success but is silently discarded on a non-priced derived node.
- Prehook `InputUnitPrice`/`ListPrice`/`UnitPrice` seed — engine re-derives and excludes derived lines.
- In-procedure DerivedPricingFormula backfill / lever-d committer / re-add V14 groups — inert on a non-priced node; a global `resultIncluded` flip mis-prices ~464–536K legacy lines.
- QuoteAction flip Amend→Renew on a OneTime line — a OneTime line can't become a priced node even flipped (refuted on the exact 2026-06-15 canary).
- Un-gate `DerivedProductsNonRenewal` — off-design, computes tier%×list (~60–71) not COLA'd Asset.Price; catastrophic blast radius.
- De-derive the RRM PBE (`IsDerived=false`) — `IsDerived` updateable=false; not the determinant.
- Naked native-uplift flip in prod — plumbing 100% empty → flat 0% = silent leak; sandbox parity-prove first.
- Mass-reprice the ~7,203 backlog — INERT on OneTime nodes AND DANGEROUS (hits Approved/Won/Ordered → invalid approvals, Workday desync, governor limits).
- Flip default PSMO `IsDefault` to TermDefined (the 2026-07-06 130-flip) — new business $0; caused live ECBM ~$44k / ACSYNM ~$2.3k regressions; reverted.
- Write `Contract.Status='Renewed'` — Revenue-Cloud-managed; after-save write faults ~27,691 activations. Keep `Renewal_Status__c='Accepted'`.
- Delete `RenewalMaintenanceAutoAddHandler` / `PartnerNetPricePosthook` wholesale — both live-wired org-wide; deactivate-first only.
- Blitz-retire the ~198 PCRs — `ProductConfigurationRule.Status` is DELETE-only; irreversible-via-API; gate on a sandbox toggle-and-revert proof.

## Verifier's 2 gate refinements (close before Stage 2/3 exit)
1. Add a Stage-2 axis confirming the target RNM assets actually carry the `AssetStatePeriod`/`AssetActionSource` the exact-Product2Id Renew join needs (114k migrated assets may lack them; `Asset.PricingSource` null org-wide).
2. Define the Stage-1 attribution failure branch: if the canary shows the old custom stack (a refuted lever) actually committed BoKS 76.57, do NOT retire the COLA stack (Stage 5) and re-open the discard-vs-commit contradiction.

## Recommended NEXT action
Ship **Stage 0 in UAT**: `Maintenance_Pricing_Failed__c` + `OrderSubmissionValidator`/`Order_Submit_Validation__mdt` extension (`Qty>0 AND UnitPrice<=0` renewal-maint block) into `Fortra_Order_Submission_Check`. Read-only w.r.t. engine price, reversible via `Active__c`. Makes silent $0 loud + Workday-blocking today while Path B is finished and parity-proven.
