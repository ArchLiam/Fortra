119a120,124
>             // NOTE: live queryTags call MUST stay inside this try/catch. The original
>             // contract swallows any queryTags failure and returns false (fail-open to
>             // Quote behavior). The response unwrap + dataPath[0] peek is extracted to the
>             // parsed-payload overload isOrderTransaction(List<Object>) so it can be unit
>             // tested with a hand-built item list; the swallow-and-continue is preserved.
136,146c141
<             if (items.isEmpty()) return false;
< 
<             // Peek at the first item's dataPath[0] — the root entity Id
<             Map<String, Object> firstItem = (Map<String, Object>) items.get(0);
<             Object dpObj = firstItem.get('dataPath');
<             if (!(dpObj instanceof List<Object>)) return false;
<             List<Object> dp = (List<Object>) dpObj;
<             if (dp.isEmpty()) return false;
< 
<             String rootId = String.valueOf(dp.get(0));
<             return rootId != null && rootId.startsWith('801');
---
>             return isOrderTransaction(items);
153a149,173
>      * @description Parsed-payload seam for Order detection. Pure of any live
>      *              Context API call so it can be unit-tested with a hand-built
>      *              SalesTransactionItem list. Returns the same sentinel (false)
>      *              the inline code returned for empty / malformed payloads, and
>      *              true only when the first item's dataPath[0] has the Order
>      *              prefix (801).
>      * @param items Parsed SalesTransactionItem node list
>      * @return true if the root entity Id has the Order prefix (801)
>      */
>     @TestVisible
>     private Boolean isOrderTransaction(List<Object> items) {
>         if (items == null || items.isEmpty()) return false;
> 
>         // Peek at the first item's dataPath[0] -- the root entity Id
>         Map<String, Object> firstItem = (Map<String, Object>) items.get(0);
>         Object dpObj = firstItem.get('dataPath');
>         if (!(dpObj instanceof List<Object>)) return false;
>         List<Object> dp = (List<Object>) dpObj;
>         if (dp.isEmpty()) return false;
> 
>         String rootId = String.valueOf(dp.get(0));
>         return rootId != null && rootId.startsWith('801');
>     }
> 
>     /**
170a191,196
>             // NOTE: live queryTags call MUST stay inside this try/catch. The original
>             // contract swallows any failure here and returns processedCount (0 so far).
>             // Only the post-query parse + core loop is extracted to the pure seams
>             // processLineItems(List<Object>, ...) / buildLineItemUpdates(...); the
>             // swallow-and-continue and the live submit + stamped-maintenance append
>             // are preserved verbatim in this scope.
206,210c232,234
<             System.debug(LoggingLevel.INFO, 'Found ' + rawLineItems.size() + ' line items');
< 
<             // Extract QLI record IDs from dataPath (QuoteActionId is not
<             // available in context tags at runtime — confirmed via debug log).
<             // Then query QLI→QuoteAction→Asset to get renewal and pricing data.
---
>             // Re-extract the QLI Ids from the parsed payload so the stamped
>             // maintenance append below has the same Id set the inline code used.
>             // (qliIds was computed inside the now-extracted parse section.)
230,244c254,261
<             // Query renewal QuoteActions via QLI relationship
<             Map<Id, QuoteLineItem> renewalQlis = new Map<Id, QuoteLineItem>();
<             if (!qliIds.isEmpty()) {
<                 for (QuoteLineItem qli : [
<                     SELECT Id, QuoteActionId, QuoteAction.Type, QuoteAction.SourceAssetId,
<                            QuoteAction.SourceAsset.Price, QuoteAction.SourceAsset.Product2Id,
<                            QuoteAction.SourceAsset.Product2.Solution_Category__c,
<                            Default_COLA_Uplift_Percent__c
<                     FROM QuoteLineItem
<                     WHERE Id IN :qliIds
<                     AND QuoteAction.Type = 'Renew'
<                 ]) {
<                     renewalQlis.put(qli.Id, qli);
<                 }
<             }
---
>             // The parse -> renewal SOQL -> contract-override -> core-loop section
>             // is extracted to the parsed-payload overload processLineItems(
>             // List<Object>, ...) -- no live Context API there -- so it can be unit
>             // tested with a hand-built item list. It returns the node updates;
>             // the live submit + stamped-maintenance append stay here.
>             List<Map<String, Object>> itemUpdates =
>                 processLineItems(rawLineItems, colaRulesMap, mycapRule);
>             processedCount = itemUpdates.size();
246,263d262
<             System.debug(LoggingLevel.INFO, 'Found ' + renewalQlis.size() + ' renewal QLIs from ' + qliIds.size() + ' total');
< 
<             // Collect Asset IDs for Contract override lookup
<             Set<Id> assetIds = new Set<Id>();
<             for (QuoteLineItem rqli : renewalQlis.values()) {
<                 if (rqli.QuoteAction?.SourceAssetId != null) {
<                     assetIds.add(rqli.QuoteAction.SourceAssetId);
<                 }
<             }
< 
<             // Get Contract overrides
<             Map<Id, ContractOverride> contractOverrides = getContractOverrides(assetIds);
< 
<             // Build updates for line items (extracted to the testable buildLineItemUpdates seam)
<             List<Map<String, Object>> itemUpdates = buildLineItemUpdates(
<                 renewalQlis, qliIdToWrapper, colaRulesMap, contractOverrides, mycapRule);
<             processedCount += itemUpdates.size();
< 
286,289c285,297
<      * @description Pure renewal-COLA seam: builds the per-line context update list
<      *              (tier resolution, explainer, buildItemUpdate) from already-resolved
<      *              maps. Extracted verbatim from processLineItems(String) so the live
<      *              path and the unit-test seam can never diverge.
---
>      * @description Parsed-payload seam for processLineItems. Given the already-
>      *              unwrapped SalesTransactionItem list, extracts QLI Ids from each
>      *              dataPath, queries the renewal QLI->QuoteAction->Asset graph,
>      *              resolves Contract overrides, and builds the list of context
>      *              node updates via the pure buildLineItemUpdates(...) loop.
>      *              Contains ZERO live Context API calls (queryTags /
>      *              updateContextAttributes) -- only QLI SOQL and the ACR SOQL
>      *              inside getContractOverrides, which are test-safe -- so the parse
>      *              + SOQL + loop section can be exercised with a hand-built item
>      *              list. Behavior is byte-for-byte equivalent to the original
>      *              inline section between the response unwrap and the submit call.
>      * @param rawLineItems Parsed SalesTransactionItem node list
>      * @return List of node-update maps (one per priced renewal line)
291a300,378
>     private List<Map<String, Object>> processLineItems(
>         List<Object> rawLineItems,
>         Map<String, COLA_Uplift_Rules__mdt> colaRulesMap,
>         MyCAP_Rules__mdt mycapRule
>     ) {
>         System.debug(LoggingLevel.INFO, 'Found ' + rawLineItems.size() + ' line items');
> 
>         // Extract QLI record IDs from dataPath (QuoteActionId is not
>         // available in context tags at runtime -- confirmed via debug log).
>         // Then query QLI->QuoteAction->Asset to get renewal and pricing data.
>         Set<Id> qliIds = new Set<Id>();
>         Map<Id, Map<String, Object>> qliIdToWrapper = new Map<Id, Map<String, Object>>();
>         for (Object rawItem : rawLineItems) {
>             Map<String, Object> itemWrapper = (Map<String, Object>) rawItem;
>             Object dpObj = itemWrapper.get('dataPath');
>             if (dpObj instanceof List<Object>) {
>                 List<Object> dp = (List<Object>) dpObj;
>                 if (!dp.isEmpty()) {
>                     try {
>                         Id qliId = Id.valueOf(String.valueOf(dp.get(dp.size() - 1)));
>                         qliIds.add(qliId);
>                         qliIdToWrapper.put(qliId, itemWrapper);
>                     } catch (Exception e) {
>                         // Not a valid Id, skip
>                     }
>                 }
>             }
>         }
> 
>         // Query renewal QuoteActions via QLI relationship
>         Map<Id, QuoteLineItem> renewalQlis = new Map<Id, QuoteLineItem>();
>         if (!qliIds.isEmpty()) {
>             for (QuoteLineItem qli : [
>                 SELECT Id, QuoteActionId, QuoteAction.Type, QuoteAction.SourceAssetId,
>                        QuoteAction.SourceAsset.Price, QuoteAction.SourceAsset.Product2Id,
>                        QuoteAction.SourceAsset.Product2.Solution_Category__c,
>                        Default_COLA_Uplift_Percent__c
>                 FROM QuoteLineItem
>                 WHERE Id IN :qliIds
>                 AND QuoteAction.Type = 'Renew'
>             ]) {
>                 renewalQlis.put(qli.Id, qli);
>             }
>         }
> 
>         System.debug(LoggingLevel.INFO, 'Found ' + renewalQlis.size() + ' renewal QLIs from ' + qliIds.size() + ' total');
> 
>         // Collect Asset IDs for Contract override lookup
>         Set<Id> assetIds = new Set<Id>();
>         for (QuoteLineItem rqli : renewalQlis.values()) {
>             if (rqli.QuoteAction?.SourceAssetId != null) {
>                 assetIds.add(rqli.QuoteAction.SourceAssetId);
>             }
>         }
> 
>         // Get Contract overrides
>         Map<Id, ContractOverride> contractOverrides = getContractOverrides(assetIds);
> 
>         // Build updates for line items. The core renewal COLA loop (tier
>         // resolution, explainer, buildItemUpdate) is extracted to the pure
>         // seam buildLineItemUpdates(...) -- no live Context API there -- so it
>         // can be unit-tested with resolved maps.
>         return buildLineItemUpdates(renewalQlis, qliIdToWrapper, colaRulesMap, contractOverrides, mycapRule);
>     }
> 
>     /**
>      * @description Pure core-loop seam for processLineItems. Given the resolved
>      *              renewal QLI map, the QLI-Id->context-wrapper map, the CMDT rules,
>      *              the Contract overrides, and the MyCAP rule, builds the list of
>      *              context node updates (one per priced renewal line). Contains
>      *              ZERO live Context API / SOQL calls, so it is unit-testable with
>      *              hand-built inputs. Behavior is byte-for-byte equivalent to the
>      *              original inline loop: same skip conditions (missing wrapper,
>      *              null lineItem, null QuoteAction, no valid asset price), same
>      *              tier hierarchy, same MyCAP flagging, same price math, same
>      *              buildItemUpdate null-skip.
>      * @return List of node-update maps (one per line item that produced an update)
>      */
>     @TestVisible
348c435
<             // CMDT default AND the Contract Override. MyCAP is NOT included —
---
>             // CMDT default AND the Contract Override. MyCAP is NOT included --
412a500
> 
417,495d504
<      * @description Parsed-payload overload of processLineItems: takes already-fetched
<      *              SalesTransactionItem wrappers instead of a context id, resolves the
<      *              renewal QLIs + contract overrides, and returns the COLA line updates.
<      *              Unit-testable seam mirroring the live String path.
<      */
<     @TestVisible
<     private List<Map<String, Object>> processLineItems(
<         List<Object> rawLineItems,
<         Map<String, COLA_Uplift_Rules__mdt> colaRulesMap,
<         MyCAP_Rules__mdt mycapRule
<     ) {
<         Set<Id> qliIds = new Set<Id>();
<         Map<Id, Map<String, Object>> qliIdToWrapper = new Map<Id, Map<String, Object>>();
<         if (rawLineItems != null) {
<             for (Object rawItem : rawLineItems) {
<                 if (!(rawItem instanceof Map<String, Object>)) continue;
<                 Map<String, Object> itemWrapper = (Map<String, Object>) rawItem;
<                 Object dpObj = itemWrapper.get('dataPath');
<                 if (dpObj instanceof List<Object>) {
<                     List<Object> dp = (List<Object>) dpObj;
<                     if (!dp.isEmpty()) {
<                         try {
<                             Id qliId = Id.valueOf(String.valueOf(dp.get(dp.size() - 1)));
<                             qliIds.add(qliId);
<                             qliIdToWrapper.put(qliId, itemWrapper);
<                         } catch (Exception e) {
<                             // Not a valid Id, skip
<                         }
<                     }
<                 }
<             }
<         }
< 
<         Map<Id, QuoteLineItem> renewalQlis = new Map<Id, QuoteLineItem>();
<         if (!qliIds.isEmpty()) {
<             for (QuoteLineItem qli : [
<                 SELECT Id, QuoteActionId, QuoteAction.Type, QuoteAction.SourceAssetId,
<                        QuoteAction.SourceAsset.Price, QuoteAction.SourceAsset.Product2Id,
<                        QuoteAction.SourceAsset.Product2.Solution_Category__c,
<                        Default_COLA_Uplift_Percent__c
<                 FROM QuoteLineItem
<                 WHERE Id IN :qliIds
<                 AND QuoteAction.Type = 'Renew'
<             ]) {
<                 renewalQlis.put(qli.Id, qli);
<             }
<         }
< 
<         Set<Id> assetIds = new Set<Id>();
<         for (QuoteLineItem rqli : renewalQlis.values()) {
<             if (rqli.QuoteAction?.SourceAssetId != null) {
<                 assetIds.add(rqli.QuoteAction.SourceAssetId);
<             }
<         }
< 
<         Map<Id, ContractOverride> contractOverrides = getContractOverrides(assetIds);
<         return buildLineItemUpdates(renewalQlis, qliIdToWrapper, colaRulesMap, contractOverrides, mycapRule);
<     }
< 
<     /**
<      * @description Parsed-payload overload of isOrderTransaction: inspects dataPath[0]
<      *              of the first item (Order roots start with 801). Pure + test-visible.
<      */
<     @TestVisible
<     private Boolean isOrderTransaction(List<Object> items) {
<         if (items == null || items.isEmpty()) return false;
<         Object first = items.get(0);
<         if (!(first instanceof Map<String, Object>)) return false;
<         Map<String, Object> firstItem = (Map<String, Object>) first;
<         Object dpObj = firstItem.get('dataPath');
<         if (!(dpObj instanceof List<Object>)) return false;
<         List<Object> dp = (List<Object>) dpObj;
<         if (dp.isEmpty()) return false;
<         String rootId = String.valueOf(dp.get(0));
<         return rootId != null && rootId.startsWith('801');
<     }
< 
< 
<     /**
811a821,822
>         Map<Id, ContractOverride> overrideMap = new Map<Id, ContractOverride>();
> 
813c824
<             return new Map<Id, ContractOverride>();
---
>             return overrideMap;
828c839
<             return new Map<Id, ContractOverride>();
---
>             return overrideMap;
835,838c846,855
<      * @description Validity-matrix seam for getContractOverrides. Builds the
<      *              ContractOverride map from already-queried AssetContractRelationship
<      *              rows so the logic (null-persist, future, expired, null-percent) is
<      *              unit-testable via in-memory rows on orgs where ACR is uninsertable.
---
>      * @description Pure validity-matrix seam for getContractOverrides. Builds the
>      *              Asset-Id -> ContractOverride map from AssetContractRelationship rows
>      *              (each carrying a Contract child relationship). Extracted as a
>      *              @TestVisible seam because the ACR link is uninsertable on RC orgs and
>      *              the live SOQL in getContractOverrides returns nothing in tests, so the
>      *              validity matrix (valid null-persist one-time override, valid future
>      *              persist, expired past persist excluded, null override percent skipped)
>      *              can only be exercised with in-memory rows. Behavior is unchanged.
>      * @param relationships AssetContractRelationship rows (queried or in-memory)
>      * @return Map of ContractOverride wrappers keyed by Asset Id
843c860
<         if (relationships == null || relationships.isEmpty()) {
---
>         if (relationships == null) {
880a898
> 
980a999,1003
>             // NOTE: live queryTags call lives inside fetchSalesTransactionItems().
>             // Keep it inside this try/catch so a context-fetch failure is swallowed
>             // and the method returns 0 (the original fail-fast contract). The pure
>             // evaluation is extracted to the parsed-payload overload
>             // processMyCAPEligibility(List<Object>, rule) so it can be unit-tested.
990a1014,1027
>     /**
>      * @description Parsed-payload seam for processMyCAPEligibility. Given the
>      *              already-fetched SalesTransactionItem list and the MyCAP rule,
>      *              runs the eligibility evaluation, flag collection, and the
>      *              MyCAPFlagApplier enqueue. Contains ZERO live Context API calls
>      *              (queryTags / updateContextAttributes) -- only QLI / attribute
>      *              SOQL and System.enqueueJob, which are test-safe -- so the real
>      *              MyCAP path can be exercised with a hand-built item list.
>      *              Behavior is preserved: same renewal gate, same qualifying
>      *              criteria, same flag-true/flag-false collection, same enqueue.
>      * @param rawLineItems Parsed SalesTransactionItem node list
>      * @param rule Global MyCAP rule CMDT record (already null/active checked)
>      * @return Number of distinct Quotes evaluated
>      */
