[
  {
    "className": "RenewalMaintenancePricingService",
    "finalPercent": 92,
    "allTestsPass": true,
    "testRunId": "707WC00002y4447",
    "uncoveredRemaining": 20,
    "deadCodeRemoved": "Removed the dead private method `getDecimal(Map<String,Object>, String)` (original lines 462-481, ~20 executable lines). Proven dead: a whole-class grep found only its own definition; all `getDecimal(` call-sites in the codebase are actually `getDecimalFromTag` in COLAUpliftPrehook/PartnerNetPricePosthook (a differently-named method). Zero callers in this class, the test class, or anywhere referencing this class's `getDecimal`.",
    "residualUncoveredExplanation": "20 lines remain uncovered, all structurally unreachable from an Apex unit test: (1) the live Context.IndustriesContext call branches that are the FALSE side of each new seam ternary/guard and only execute inside a real pricing transaction \u2014 157-161 (buildContext), 170+172 (persistContext + its catch), 234-236 and 293-295 (the two updateContextAttributes writes), 412 (apiResponse==null after a live queryTags); and (2) defensive continue/early-return guards inside the item-update loops that cannot fire for a valid candidate \u2014 194 (colaNetByLineId.isEmpty), 207 (colaNet-not-in-map continue), 213+229 (updatePath==null continue + resulting empty-updates return), 261 (colaNet<=0 continue in finalize), 267 (updatePath==null continue in finalize), 286 (empty-updates return in finalize). These are logically dead once a line has passed filterLinesNeedingColaNet (which already guarantees computeColaNet>0) and because real QLI records always have non-null QuoteId/Id. No further seam can reach them without changing production logic.",
    "prodSeamDiff": "Five minimal, behavior-preserving edits to RenewalMaintenancePricingService.cls (production path with overrides null is byte-for-byte identical to the original); all verified live on FortraUAT.\n\nSEAM FIELDS (added after CONTEXT_DEFINITION_NAME):\n  @TestVisible private static Map<String, Object> testQueryTagsResponse;\n  @TestVisible private static String testContextIdOverride;\n(both default null; every guard below also requires Test.isRunningTest())\n\nREAD seam \u2014 queryLineItems() (~line 405):\n  Map<String, Object> apiResponse = (Test.isRunningTest() && testQueryTagsResponse != null)\n      ? testQueryTagsResponse\n      : ctx.queryTags(new Map<String, Object>{ 'contextId' => contextInstanceId, 'tags' => new List<String>{ 'SalesTransactionItem' } });\n\nWRITE seam \u2014 applyToContext() updateContextAttributes (~line 232):\n  Map<String, Object> updateResult = (Test.isRunningTest() && testQueryTagsResponse != null)\n      ? new Map<String, Object>{ 'isSuccess' => true }\n      : ctx.updateContextAttributes(new Map<String, Object>{ 'contextId' => contextInstanceId, 'nodePathAndAttributes' => itemUpdates });\n\nWRITE seam \u2014 applyColaNetFinalizeToContext() updateContextAttributes (~line 291): identical guard wrapping the same ctx.updateContextAttributes(...) call.\n\nBUILD/PERSIST seam \u2014 finalizeRenewalMaintenanceAfterPricing() (~lines 153-171):\n  String contextId;\n  if (Test.isRunningTest() && testContextIdOverride != null) { contextId = testContextIdOverride; }\n  else { Map<String,Object> buildResult = ctx.buildContext({contextDefinitionName, sourceRecordId}); contextId = buildResult != null ? String.valueOf(buildResult.get('contextId')) : null; }\n  ... applyToContext(quoteId, contextId);\n  try { if (!(Test.isRunningTest() && testContextIdOverride != null)) { ctx.persistContext({'contextId'=>contextId}); } } catch (Exception e) { ...debug... }\n\nDEAD CODE: removed unused private getDecimal(Map,String) (original 462-481), zero callers proven by grep.\n\nNo public/@InvocableMethod signatures, names, or logic were changed.",
    "summary": "Raised RenewalMaintenancePricingService from 50.4% (128/254) to 92.0% (230/250) coverage, all green, verified live on FortraUAT (Test Run Id 707WC00002y4447, 50 tests, 100% pass). Re-retrieved the latest class+test fresh from UAT first. Added two @TestVisible static override fields and four behavior-preserving seams (queryTags read, two updateContextAttributes writes, buildContext+persistContext) that swap in a fabricated response ONLY under Test.isRunningTest() with the override set \u2014 production path unchanged. Removed dead getDecimal(). Extended the existing 37-method test (kept all green) with 13 new methods that drive applyToContext, applyColaNetFinalizeToContext, finalizeRenewalMaintenanceAfterPricing, the three InvocableMethod dispatch branches, and all queryLineItems parse/skip branches end-to-end against fabricated queryTags-shaped responses (queryResult.SalesTransactionItem[].tagValue/dataPath), including malformed-item skips and both getLineItemRecordId null paths. The 20 residual uncovered lines are the live-ctx-call false-ternary branches and unreachable defensive guards \u2014 structurally impossible to hit from a unit test. Edited files: /Users/liamjeong/Documents/Code/Fortra/Data/sc-maint/src/classes/RenewalMaintenancePricingService.cls and RenewalMaintenancePricingServiceTest.cls."
  },
  {
    "className": "PartnerNetPricePosthook",
    "finalPercent": 95.53,
    "allTestsPass": true,
    "testRunId": "707WC00002y4NCr",
    "uncoveredRemaining": 23,
    "deadCodeRemoved": "None. No production logic was deleted. The provably-dead defensive guards the prompt flagged (253-254/256-257, 376, 427, 835) were left in place to keep the prod diff a pure set of guarded test seams with zero behavior risk; they remain as residual uncovered lines (now 277-281, 400, 872) and are documented as unreachable rather than removed.",
    "prodSeamDiff": "Prod class PartnerNetPricePosthook.cls \u2014 additive, behavior-preserving seams only (every production path, with all overrides null as they always are in prod, is byte-for-byte identical to before):\n\n1. Annotation only: `private static Boolean isProcessing` -> add `@TestVisible` (no behavior change; lets a test set the re-entrancy flag).\n\n2. Five new @TestVisible static fields (default null; only ever set by the test class):\n   @TestVisible private static Map<String,Object> testLineItemsResponse;   // queryTags('SalesTransactionItem') stub\n   @TestVisible private static Map<String,Object> testQuoteHeaderResponse;  // queryTags('SalesTransaction') stub\n   @TestVisible private static Map<String,Object> testBuildContextResponse; // buildContext stub (order path)\n   @TestVisible private static List<Map<String,Object>> testCapturedItemUpdates; // captured submit payload for assertions\n\n3. applyNetPricesToOrder() \u2014 buildContext READ guarded:\n   Map<String,Object> buildResult = (Test.isRunningTest() && testBuildContextResponse != null) ? testBuildContextResponse : ctx.buildContext({...});\n   and persistContext WRITE guarded:\n   if (!(Test.isRunningTest() && testBuildContextResponse != null)) { ctx.persistContext({'contextId'=>contextId}); }\n\n4. queryQuoteHeaderFromContext() \u2014 queryTags READ guarded:\n   Map<String,Object> apiResponse = (Test.isRunningTest() && testQuoteHeaderResponse != null) ? testQuoteHeaderResponse : ctx.queryTags(queryInput);\n\n5. queryLineItems() \u2014 queryTags READ guarded:\n   Map<String,Object> apiResponse = (Test.isRunningTest() && testLineItemsResponse != null) ? testLineItemsResponse : ctx.queryTags(queryInput);\n\n6. submitContextUpdates() \u2014 updateContextAttributes WRITE guarded (early return that captures payload only when a read was injected):\n   if (Test.isRunningTest() && (testLineItemsResponse != null || testBuildContextResponse != null)) { testCapturedItemUpdates = itemUpdates; return; }\n   ...else the original try { ctx.updateContextAttributes(updateInput); } catch(...) {...} runs unchanged.\n\nIn production all five overrides are null, so every ternary takes the `ctx.*` branch and every guard's condition is false -> identical runtime behavior. Confirmed: re-retrieve of the deployed class equals the local file (no drift).",
    "residualUncoveredExplanation": "23 residual uncovered lines, all of which are unreachable from an Apex unit test by construction (verified line-by-line):\n- The real platform-call branches the seam preserves (override-null path): 119-121 ctx.buildContext, 132 ctx.persistContext, 523 ctx.queryTags (header), 723-724 + 725-730 ctx.updateContextAttributes real-write try/catch incl. the 'not in updatable state' handler and 'throw e'. These throw outside a live RCA pricing transaction, so they cannot run in a unit test.\n- 'Real API returned null/neither-key' defensive returns surrounding those calls: 525, 538, 587 (catch), 589, 641. The seam can only inject a non-null map, so the null-response/catch branches stay bound to the live API.\n- Provably-dead defensive guards (stricter upstream guard makes them unreachable): 277-281 (shadowed by shouldSkipProcedureOwnedPartner), 400 (currentPrice<=0 after needsDeferredPartnerPricing already guaranteed >0), 451, 872 (lineTotal==null after colaNet>0 guarantees resolveColaLineTotal non-null). Left in place rather than deleted to keep the diff pure-seam.",
    "summary": "Raised PartnerNetPricePosthook from 69.2% to 95.53% (492/515 lines), with all 64 PartnerNetPricePosthookTest methods green (run 707WC00002y4NCr, 64 completed / 0 failed) on FortraUAT. Re-retrieved the latest class+test fresh from UAT first (matched the existing src copies \u2014 no churn). Added a minimal, behavior-preserving test seam: 5 @TestVisible static override fields + an @TestVisible on isProcessing, with guarded ternaries on the Context.IndustriesContext READs (buildContext / two queryTags) and guarded skips on the WRITEs (persistContext / updateContextAttributes), so the JSON parsers (queryLineItems, queryQuoteHeaderFromContext), the per-line apply loop (applyPartnerNetPrices incl. partner-net and renewal-maintenance COLA paths), execute(), applyNetPricesToOrder()'s order path, the @InvocableMethod, and submitContextUpdates all run under test with fabricated queryTags-shaped responses. No production logic was changed, renamed, or removed; all overrides are null in production so behavior is byte-for-byte identical (confirmed by re-retrieving the deployed class = local). Extended the existing test class with ~14 new methods asserting real outcomes (built itemUpdates contents, commercial-net pull-down to partner tier, COLA net 67.38, parser guard/skip branches, order buildContext path, error-swallow path). No dead code removed by choice \u2014 the flagged dead guards are documented as unreachable residuals to keep the prod diff a pure seam. The 23 remaining uncovered lines are exactly the live-platform-call branches and their null-response/catch guards that cannot be exercised in an Apex unit test. Files: /Users/liamjeong/Documents/Code/Fortra/Data/sc-maint/src/classes/PartnerNetPricePosthook.cls and PartnerNetPricePosthookTest.cls (both deployed to FortraUAT)."
  },
  {
    "className": "MaintenanceOrderDecompositionService",
    "finalPercent": 97.9,
    "allTestsPass": true,
    "testRunId": "707WC00002y4kwM",
    "uncoveredRemaining": 5,
    "deadCodeRemoved": "None. Every private method (resolveCommercialUnitPrice, loadDerivedPricebookEntryIds, loadMaintenanceTierByQliId, normalizeTierValue, resolveNetPrice) and every public/@TestVisible member has a proven in-class caller (grep-verified), so there was no dead code to remove.",
    "prodSeamDiff": "ZERO seam. The production class MaintenanceOrderDecompositionService.cls is byte-for-byte identical to the fresh UAT retrieve (verified with `diff` against /Users/liamjeong/Documents/Code/Fortra/Data/sc-maint/fresh_MaintenanceOrderDecompositionService/.../MaintenanceOrderDecompositionService.cls \u2014 no output, exit 0). As the prompt anticipated, this class has NO Context.IndustriesContext I/O (grep = 0 hits), so no @TestVisible override field, no Test.isRunningTest() guard, and no behavior change was introduced. Only the test class MaintenanceOrderDecompositionServiceTest.cls was edited and deployed. Production behavior is therefore guaranteed unchanged.",
    "residualUncoveredExplanation": "5 lines remain uncovered, all inherently un-coverable by an Apex unit test, not behavior I could reach: (1) Line 215 \u2014 the defensive `else { patches.put(orderItemId, decompPatch); }` standalone-add in buildCommercialPatchesFromWork. Both the decomposition loop and the commercial loop iterate the SAME work.allOrderItems, and any decomposition patch always carries a positive UnitPrice (resolveNetPrice floors at maintenanceBase > 0), so the commercial loop never `continue`s for a decomposable line \u2014 meaning decompositionPatches can never contain a key absent from `patches`. The branch is dead/defensive given a self-consistent OrderMaintenanceWork. (2) Lines 364,365,366,370 \u2014 the loadMaintenanceTierByQliId dedup branch that prefers the 'Maintenance Type Defn' attribute when a QLI already has a tier value. Covering it requires a QuoteLineItem with 2+ tier-valued QuoteLineItemAttribute rows. Live UAT query returned ZERO tier-valued QuoteLineItemAttribute rows in the org, and EntityParticle shows QuoteLineItemAttribute.IsCreatable=false (managed RLM object, not DML-insertable), so the multi-attribute dedup case cannot be seeded in a test. The single-attribute path (lines 357-361) IS covered.",
    "summary": "Raised MaintenanceOrderDecompositionService solo coverage from 77% (180/233) to 97.9% (228/233), all 53 tests green, with ZERO production-class changes. Approach: the class has no pricing-engine Context I/O, so no seam was needed \u2014 the uncovered lines were reachable by ordinary unit tests. I extended MaintenanceOrderDecompositionServiceTest with 20 new behavioral tests that construct the @TestVisible OrderMaintenanceWork object in memory (real inserted Order/OrderItem/QuoteLineItem rows for genuine Ids, with derivedPbeIds/tierByQliId populated directly), making the derived-maintenance decomposition (lines 126-153), commercial-patch merge/standalone (176-219), resolveCommercialUnitPrice fallbacks (223-236), seedFromWork branches (256-290), public seedOrderItemsFromQuote/persistFromWork DML paths, and the renewal non-new-business loadWork return (88-89) all deterministically reachable without depending on org-specific derived-PBE data. NetUnitPrice (a non-writeable RLM field) is injected via a JSON round-trip helper. Quote_Checklist_Item__c is seeded on every Quote insert to satisfy the checklist VR, mirroring the existing setup. Residual 5 lines are genuinely un-coverable (1 defensive dead branch + 4 dedup lines needing QuoteLineItemAttribute data that doesn't exist and can't be DML-inserted). Files: /Users/liamjeong/Documents/Code/Fortra/Data/sc-maint/src/classes/MaintenanceOrderDecompositionServiceTest.cls (edited+deployed); /Users/liamjeong/Documents/Code/Fortra/Data/sc-maint/src/classes/MaintenanceOrderDecompositionService.cls (UNCHANGED, byte-identical to UAT). Final solo run: Test Run Id 707WC00002y4kwM, Outcome Passed, 53/53."
  }
]