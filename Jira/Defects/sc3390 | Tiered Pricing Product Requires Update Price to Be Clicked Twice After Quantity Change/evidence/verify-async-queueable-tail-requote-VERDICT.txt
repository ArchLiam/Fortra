SC-3390 ADVERSARIAL VERIFICATION — CANDIDATE [async-queueable-tail-requote]
Generated: 2026-06-11 (read-only, FortraUAT)

CANDIDATE: Post-place async Queueable (COLAUpliftPrehook.MyCAPFlagApplier) writes
data that retriggers a reprice over a stale context, masking the first-click result.
Candidate's own stance: "currently excluded" — queueable now writes only Quote.Mycap__c.

VERDICT: REFUTED (as the active driver of SC-3390). The candidate's "currently
excluded" self-assessment is CORRECT and is now proven against the LIVE org, not just
the local mirror. No post-place queueable performs QLI DML or retriggers pricing.

=== EVIDENCE ===

1. LIVE class verified (NOT stale local mirror).
   - COLAUpliftPrehook live Id 01pWC000001wNGbYAM, ApiVersion 65, len 43272,
     LastModified 2026-06-11T14:13:15Z by Nir Kailash (modified TODAY).
   - Local mirror Org Data/_src/classes/COLAUpliftPrehook.cls DIFFERS from live
     (546 comment-stripped diff lines, 1253 vs 1359 lines). Local could not be trusted;
     live body retrieved to verify-async-queueable-tail-requote-LIVE-COLAUpliftPrehook.cls.
   - LIVE MyCAPFlagApplier.execute() lines 1157-1192: writes ONLY Quote.Mycap__c via
     Database.update(toUpdate,false) where toUpdate is List<Quote>. NO QLI DML. NO place/
     reprice call. The cited "QLI DML removed" comment is present in LIVE at 1158-1166.

2. The enqueue is RENEWAL-GATED — does not even run on the SC-3390 repro path.
   - Live line 907: "if (stActionType != 'Renew') continue;" — only lines with context
     tag SalesTransactionActionType=='Renew' are collected.
   - Live line 951-954: "if (allRenewalWrappers.isEmpty()) { ...'no renewal lines found'; return 0; }"
     => For a NEW tiered-pricing quote (SC-3390 = change Quantity on a tiered product,
     NOT a renewal), the MyCAP branch returns BEFORE the enqueue at line 1093. The
     queueable is never created on the primary repro path.

3. Even when it DOES enqueue (renewal quote), it performs NO DML and NO reprice.
   Proven from BOTH queueable executions captured:
   a) queueable_handler_log.txt (07LWC00000OrigP2AR, 12:45:09, paired w/ aura place 12:45:03):
      - Only SOQL: SELECT Id, Mycap__c FROM Quote (line 1282). Quote Mycap__c=false.
      - desired=false, current=false => desired!=current is FALSE => toUpdate stays [].
      - Line 1289 toUpdate.isEmpty() check => NO Database.update executed.
      - ZERO DML_BEGIN/DML_END, ZERO place/reprice callout, ZERO QuoteLineItem reference.
   b) verify-async-queueable-tail-requote-QH-0434.txt (07LWC00000OpZsT2AV, 04:34:18,
      paired w/ the actual place_action_log 04:34:13 on quote 0Q0WC000003671t0AA):
      - Same: toUpdate=[], desired=false, current=false, Quote SELECT only, no DML, no place.
      - Profiling confirms only the Quote SELECT ran; no DML statements.

4. The synchronous place transaction (place_action_log.txt, log 07LWC00000OqoxN2AR) shows:
   - Exactly ONE enqueueJob (line 19279/19282), constructing COLAUpliftPrehook.MyCAPFlagApplier.
   - "MyCAP: enqueued - Quotes flag=true:0 flag=false:1" (line 19298) — flagFalse path,
     which in the queueable resolves to a no-op (current already false).
   - ZERO DML_BEGIN events in the entire place transaction (no QLI DML to re-enter pricing).
   - The prehook AttributeVolumePricingPrehook fires exactly once (START=1, END=1,
     'Skipping recursive execution'=0) — no recursion, no second pass.

5. Causality timeline refutes "retrigger": every place log is followed by exactly one
   small (~7-9.6KB) QueueableHandler transaction, and NO QueueableHandler is followed by
   another /place. The async tail does not enqueue a follow-on pricing run. (apexlog-list.txt)

6. Other post-place async actors ruled out for SC-3390:
   - RenewalQuoteLineHandler.RenewalQuoteLineCleanupQueueable does a QLI delete (cls 129)
     but is gated on QLI AFTER-INSERT for renewal carryover cleanup, NOT on a
     quantity-change reprice of an existing line. Not present in the place enqueue list.
   - No @future / executeBatch enqueued in the place transaction.

=== CONFIRM SIGNATURE (would have confirmed candidate) — NOT FOUND ===
   No log shows MyCAPFlagApplier (or any post-place queueable) performing QuoteLineItem
   DML, and no follow-on /place pricing run is enqueued by the tail. The exact REFUTE
   signature the candidate predicted (cls writes only Quote.Mycap__c, QLI DML removed)
   is what the live org actually shows.

=== RESIDUAL / REGRESSION RISK ===
   The candidate is correctly flagged as a latent regression class: this codebase HAS had
   the DML->reprice->DML 'prices aren't up to date' bug (cls comment 1158-1166), and the
   guard is a code comment, not a structural barrier. If a future edit re-adds QLI DML to
   MyCAPFlagApplier (or any post-place queueable), the 'click twice' family could return
   FOR RENEWAL QUOTES. But SC-3390's symptom is on tiered NEW quotes where this branch
   never runs, so it cannot be the SC-3390 driver even as a regression.
